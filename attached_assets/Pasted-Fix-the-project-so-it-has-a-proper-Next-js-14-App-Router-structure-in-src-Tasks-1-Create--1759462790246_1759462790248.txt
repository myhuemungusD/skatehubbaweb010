Fix the project so it has a proper Next.js 14 App Router structure in `src/`.

Tasks:
1. Create `src/app/` with:
   - layout.tsx (global layout with Tailwind, imports globals.css, wraps children)
   - globals.css (basic Tailwind reset + font)
   - page.tsx (homepage with graffiti background, SkateHubba logo, tagline “Own Your Tricks”, nav links to Login, Profile, Shop, Closet, Map)
   - login/page.tsx (Google + Email/Password auth page using Firebase client SDK and shadcn/ui buttons, card layout, SkateHubba theme colors)
   - profile/page.tsx (Profile page showing avatar, displayName, email, XP progress bar, edit modal with avatar upload to Firebase Storage and Firestore update, redirect to /login if not authed)
   - shop/page.tsx (Shop page with shop background, SkateHubba style header, nav links back to Home/Profile)
   - closet/page.tsx (Closet page with profilecloset.png background, placeholder for avatar customization, nav links)
   - map/page.tsx (Map page placeholder, SkateHubba green header, description “check-ins coming soon”, nav links)

2. Add supporting files:
   - src/lib/firebase.ts (Firebase client init, imports firebase/app, auth, firestore, storage; config from env; export auth, db, storage, googleProvider, serverTimestamp)
   - src/lib/userService.ts (ensureUserDoc, fetchUserDoc, updateProfile with avatar upload to Firebase Storage)
   - src/lib/utils.ts (cn function using clsx + tailwind-merge)
   - src/store/userStore.ts (Zustand store: user, loading, bootstrap, signInGoogle, signInEmail, signOut, refreshUserDoc, updateDisplayNameLocal)

3. Add shadcn/ui components in `src/components/ui/`:
   - button.tsx, card.tsx, input.tsx, label.tsx, avatar.tsx, dialog.tsx, progress.tsx

4. Place `.env.example` in root with NEXT_PUBLIC_FIREBASE_* keys and `.gitignore` including `.env.local`.

5. Keep everything production-ready: no mock data, no TODOs, no placeholders. All routes must compile with `npm run build`.

Goal: after running `npm run dev`, I should be able to visit `/`, `/login`, `/profile`, `/shop`, `/closet`, `/map` and see fully styled SkateHubba pages wired to Firebase client SDK and Zustand.
