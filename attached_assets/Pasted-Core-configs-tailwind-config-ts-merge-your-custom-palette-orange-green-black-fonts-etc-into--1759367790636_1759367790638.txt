Core configs

tailwind.config.ts → merge your custom palette (orange/green/black, fonts, etc.) into the new Tailwind config generated by create-next-app.

firestore.rules → drop at the project root.

.env.example → from my skatehubbagrok_full_with_PR.zip, place at root.

2. Brand assets

From your current repo uploads / branding folder:

public/LOGOmain.png

public/logo (2).png

public/shop background.png

public/profile background.png

public/profilecloset.png

public/hubbagraffwall.png

Put all of these in the public/ folder in Next.js (so you can reference them with /LOGOmain.png, etc.).

3. UI + Auth modules

Copy in from the prepared package:

src/lib/firebase.ts

src/lib/userService.ts

src/lib/utils.ts

src/store/userStore.ts

src/app/login/page.tsx

src/app/profile/page.tsx

src/components/ui/* (button, card, input, label, avatar, dialog, progress)

4. Homepage scaffold (Next.js App Router)

Create or replace src/app/page.tsx:

import Image from "next/image";
import Link from "next/link";

export default function HomePage() {
  return (
    <main className="relative min-h-screen bg-black text-white">
      {/* Background graffiti wall */}
      <Image
        src="/hubbagraffwall.png"
        alt="Graffiti wall"
        fill
        className="object-cover opacity-60"
        priority
      />

      <div className="relative z-10 flex flex-col items-center justify-center min-h-screen text-center space-y-6">
        {/* Logo */}
        <Image
          src="/LOGOmain.png"
          alt="SkateHubba logo"
          width={240}
          height={240}
          priority
        />

        <h1 className="text-3xl font-bold text-[#ff6a00] drop-shadow-lg">
          Own Your Tricks
        </h1>

        {/* Menu links */}
        <nav className="flex space-x-6 mt-6">
          <Link href="/login" className="px-6 py-3 rounded-xl bg-[#24d52b] text-black hover:bg-[#1fb125]">
            Login
          </Link>
          <Link href="/profile" className="px-6 py-3 rounded-xl bg-[#ff6a00] text-black hover:bg-[#e55f00]">
            Profile
          </Link>
          <Link href="/shop" className="px-6 py-3 rounded-xl bg-neutral-900 border border-neutral-700 hover:bg-neutral-800">
            Shop
          </Link>
        </nav>
      </div>
    </main>
  );
}

5. Verify
cp .env.example .env.local   # fill Firebase values
npm run dev
